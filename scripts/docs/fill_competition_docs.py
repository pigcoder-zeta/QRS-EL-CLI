from __future__ import annotations

from shutil import copyfile
from pathlib import Path
from docx import Document

ROOT = Path(__file__).resolve().parents[2]
BASE = ROOT / "计设提交材料汇总 (1)"
TITLE = "Argus：基于神经符号融合的多 Agent 协同自动化漏洞检测系统"
TODAY = "2026年4月17日"
PLACEHOLDER = "【待补】"

SUMMARY_100 = "Argus以神经符号融合为核心，协同多Agent、CodeQL与Docker沙箱，构建从规则生成、静态检测到动态验证的自动化漏洞检测闭环。"
INNOVATION_100 = "创新提出‘受控智能+精确分析+动态实证’范式，实现多Agent闭环、自修复规则合成、防投毒RAG与证据链输出。"

REPORT_OVERVIEW = """Argus 聚焦静态应用安全测试长期存在的三类核心矛盾：传统规则引擎误报率高、规则编写与维护门槛高、检测结果缺乏可验证证据。针对上述问题，本作品提出一种基于神经符号融合的多 Agent 协同自动化漏洞检测系统，将大语言模型的能力限定在“规划—生成—研判—编排”的高层智能环节，将精确的数据流追踪、污点分析与规则执行交由 CodeQL 等符号分析能力完成，并通过 Docker 沙箱对漏洞可利用性进行运行时验证，构建出“自主规划—规则合成—静态检测—语义审查—动态验证”的全链路闭环。

与将大模型直接用于漏洞判断的方案不同，Argus 强调“可控智能”而非“自由生成”，强调“证据链输出”而非“结论黑箱”，强调“工程稳定性”而非“单点能力展示”。系统原生支持多语言、多漏洞类型和多类代码库场景，能够输出覆盖漏洞位置、数据流线索、置信度解释、PoC 验证结果与处置建议的结构化分析结果，适用于 DevSecOps 安全左移、代码审计、攻防演练、安全教学与科研验证等场景，具备较强的工程落地潜力与赛事展示价值。"""

REPORT_BACKGROUND = """（1）行业背景。静态应用安全测试是软件安全体系的重要基础能力，但传统 SAST 工具长期面临“高误报、高门槛、高复核成本”的结构性问题。对于企业和开发团队而言，海量低质量告警会显著增加人工审查压力，甚至造成高危漏洞被淹没在噪声之中；而高质量规则的构建又依赖资深安全专家，难以适应多语言、多框架和快速演化的软件生态。

（2）技术现状。当前主流方案大致可分为三类：其一是以 CodeQL、Fortify、Semgrep 等为代表的传统规则型工具，优势在于分析过程严谨、结果具有可解释性，但规则开发成本高、误报控制依赖长期知识积累；其二是以大模型为核心的智能审查方案，优势在于自然语言理解和上下文归纳能力较强，但容易产生幻觉、输出不稳定、难以直接作为可信安全结论；其三是集成式安全平台，虽然具备一定流程化与平台化能力，但通常缺少对 AI 输出的强约束机制，也缺少从静态发现到动态验证的完整闭环。

（3）问题定义。人工智能在安全分析中的真正价值，不应停留在“替代人工写结论”，而应体现在“提升规则生成效率、增强上下文理解能力、降低误报、强化验证能力”上。因此，本作品要解决的不是单一检测精度问题，而是构建一个兼具智能性、严谨性、可验证性与可落地性的下一代漏洞检测系统。Argus 正是在这一目标下提出：让 LLM 负责受控推理，让符号分析负责精确执行，让动态验证负责最终佐证，从系统层面解决“检不准、写不出、证不了”的行业难题。"""

REPORT_DESIGN = """本作品采用“神经符号融合 + 多 Agent 协同闭环”的总体架构。系统首先由 Agent-P 对目标代码库进行侦察与规划，自动识别语言构成、框架特征、高风险依赖、入口点与潜在攻击面，并生成面向具体项目的扫描计划；随后 Agent-T 基于代码库画像完成智能分诊，动态配置提示策略、上下文窗口与推荐漏洞类型；Agent-Q 在黄金模板、规则记忆与编译器反馈约束下自动完成 CodeQL 查询生成与自修复；Agent-R 对静态发现进行源码级语义审查，给出置信度、误报判断与分析解释；Agent-S 依据漏洞类型自动生成验证 PoC；Agent-E 则在 Docker 沙箱中部署目标环境并执行利用验证，输出运行时证据。

这一设计的关键在于，Argus 不是把多个功能简单堆叠，而是将每个 Agent 置于明确职责边界和标准化数据接口之上，使其共同组成可扩展、可替换、可并行调度的智能安全工作流。系统在知识层面进一步构建了带信任分级的 Rule Memory 规则记忆库，将历史规则、漏洞利用链路摘要与上下文语义特征统一组织为可检索知识，并通过来源审计、完整性校验与签名机制降低知识投毒风险。整体来看，Argus 的设计理念是“让 LLM 受控地思考，让符号分析精确地执行，让动态验证不可伪造地证明”，从而实现准确性、稳定性与可信性的统一。"""

REPORT_TECH = """1. 自主多 Agent 决策架构。系统设计 Agent-P/T/Q/R/S/E 六类智能体，分别承担侦察规划、智能分诊、规则合成、语义审查、PoC 生成和沙箱执行等任务，形成覆盖全流程的自主协同检测闭环。

2. 神经符号融合分析机制。作品将 LLM 的优势用于高层策略生成、上下文理解和语义研判，将 CodeQL 的优势用于精确的数据流分析和规则执行，以职责分离的方式实现智能性与严谨性的统一。

3. 自修复规则合成机制。针对 LLM 直接生成 CodeQL 规则存在编译失败、API 调用错误和语义不稳定的问题，系统构建多语言黄金模板库，并引入“生成—编译—反馈—修复”的自动迭代链路，显著提升规则的首次可用率和工程鲁棒性。

4. 防投毒 RAG 漏洞知识库。作品围绕安全场景下的检索增强可信性问题，引入信任分级、来源审计、完整性校验与签名验证等机制，使规则记忆既可积累、可复用，又具备较强的安全防护能力。

5. 全链路动态验证机制。通过 Agent-S 与 Agent-E 的协同，系统能够针对静态发现自动构造验证请求、部署容器化环境并执行 PoC 测试，使检测结果从“推测存在漏洞”升级为“具备运行时证据的漏洞结论”。

6. 可视化与工程一体化实现。系统提供前后端联动的可视化控制台，用于展示任务进度、Agent 协同关系、漏洞证据链与指标对比结果，提升作品的演示表达力与实际应用可用性。"""

REPORT_IMPL = """在工程实现层面，项目后端采用 Python 构建多 Agent 协调与安全分析流水线，核心模块涵盖工作流协调器、CodeQL 运行封装、规则记忆管理、源码浏览、结果导出与扫描状态管理等。系统支持从仓库地址或本地代码启动分析流程，能够完成数据库构建、规则匹配与生成、静态扫描、结果审查、动态验证和报告输出等关键步骤。为提升工程稳定性与可复用性，系统还实现了数据库缓存、构建策略探测、结果标准化、异常兜底与阶段化解耦等机制。

前端部分基于 Vue 构建可视化安全控制台，围绕系统中枢、扫描任务、漏洞取证、规则库、对抗验证与指标展示等视图组织交互逻辑，重点强化“AI 决策过程可视化”“多 Agent 协同过程可感知”“漏洞证据链可追踪”的展示体验。整体实现并非停留在算法验证层，而是兼顾研究原型、系统工程和竞赛演示三重目标，使作品既有技术深度，也具备较强的展示完成度。"""

REPORT_TEST = """为验证系统有效性，作品在公开安全基准与真实工程场景中开展了多维测试。根据现有项目材料，Argus 在 OWASP Benchmark v1.2（共 2740 个测试样例，覆盖多个典型 CWE 类别）上的综合表现达到 F1=0.890、精确率 90.4%、误报率 10.0%。与原始 CodeQL 引擎相比，作品在保持检测能力的同时显著降低误报，体现出多 Agent 协同与神经符号融合路线在安全场景中的现实价值。

除总体指标外，项目还通过组件贡献度与消融分析验证各模块的独立作用：智能分诊提升扫描聚焦能力，规则记忆与模板约束提高规则可用率，语义审查降低误报，动态验证增强结果可信性。结合真实项目扫描和前端可视化展示，能够较好证明系统在准确性、稳定性、解释性、复核友好性与工程落地性等方面的综合优势。对于竞赛评审而言，这类“有指标、有对照、有闭环证据”的验证方式，更能体现作品不仅“能做出来”，而且“经得起验证”。"""

REPORT_SUMMARY = """Argus 以“受控智能、精确分析、动态实证”为核心主线，将多 Agent 协同机制、大模型语义能力、CodeQL 符号分析与 Docker 沙箱验证进行系统级整合，形成了一套兼具创新性、工程性与可信性的人工智能安全应用方案。相较传统工具，作品提升了漏洞检测的精确率、结果解释性与复核效率；相较纯大模型方案，作品通过规则约束、符号执行和动态验证显著降低了幻觉风险与结论不稳定性。

从赛事评价维度看，本作品具有较完整的问题定义、明确的技术创新路径、可运行的系统实现、可量化的测试结果以及清晰的应用推广前景，能够较好体现人工智能实践赛道所强调的“技术创新有深度、系统实现有完成度、应用价值有说服力、展示效果有可感知性”的综合特征。"""

REPORT_FEATURES = """（1）提出“受控智能 + 精确分析 + 动态实证”的总体范式，将神经网络能力与符号分析能力进行系统级融合，而非简单功能拼接。
（2）设计覆盖规划、分诊、规则合成、语义审查、PoC 生成与动态执行的六类 Agent 协同架构，形成完整自动化闭环。
（3）引入自修复规则合成与防投毒规则记忆库，兼顾生成效率、工程稳定性与安全可信性。
（4）通过 Docker 沙箱验证与证据链输出机制，将传统静态告警升级为可复查、可证明、可演示的漏洞结论。"""

REPORT_PROMOTION = """本作品可应用于企业级 DevSecOps 安全左移、开源项目安全巡检、教学实验平台、网络安全竞赛训练、攻防演练与科研验证等多类场景。对于企业而言，Argus 可作为漏洞初筛与证据增强平台，降低安全团队在误报过滤与人工复核上的成本；对于高校与实验教学而言，系统能够以可视化方式展示数据流、漏洞链路与验证过程，兼具教学示范性与科研训练价值；对于赛事展示而言，作品具备鲜明的“AI 驱动 + 工程闭环 + 安全实证”特征，便于形成强展示记忆点。若进一步与 CI/CD、漏洞管理平台和组织级规则治理体系结合，作品具备向实战化智能安全平台演进的潜力。"""

REPORT_FUTURE = """未来，Argus 可沿三个方向持续深化：其一，扩展对更多语言、框架与复杂安全场景的原生支持，提升跨域检测能力；其二，引入更细粒度的反馈学习与策略优化机制，让 Agent 协同从规则驱动进一步走向数据驱动与自适应优化；其三，加强与持续集成平台、漏洞生命周期管理系统和企业安全运营体系的集成能力，构建面向真实生产环境的智能安全分析基础设施。通过这些演进，Argus 有望从竞赛原型进一步发展为具有持续应用价值的智能安全平台。"""

SUMMARY_REFERENCES = [
    "1. GitHub CodeQL：基于语义与数据流分析的代表性静态分析框架。",
    "2. Semgrep：面向开发者的轻量级静态规则扫描工具。",
    "3. OWASP Benchmark：国际通用的漏洞检测评测基准。",
]

FILES = [
    ("Argus_作品报告.pdf", "需导出为 PDF 后上传", "团队原创 / 如含第三方图片或图标需单独核验来源"),
    ("Argus_作品信息概要.pdf", "需导出为 PDF 后上传", "团队原创"),
    ("Argus_源代码.zip", "建议压缩后上传", "项目自研代码 + 开源依赖，需在说明中注明"),
    ("Argus_演示视频.mp4", "需录制后上传", "团队原创"),
    ("Argus_PPT.pdf", "如提交答辩 PPT 则上传", "团队原创 / 图示素材需核验来源"),
    ("Argus_安装部署与用户手册.pdf", "建议补充后上传", "团队原创"),
]


def set_cell(cell, text: str):
    cell.text = text


def fill_summary_doc():
    src = BASE / "05-2 作品信息概要表（人工智能实践赛、实践赛）模板.docx"
    dst = BASE / "05-2 作品信息概要表（人工智能实践赛、实践赛）填写版-精修终稿.docx"
    copyfile(src, dst)
    doc = Document(dst)
    t = doc.tables[0]

    set_cell(t.cell(0, 0), "作品名称")
    set_cell(t.cell(0, 7), TITLE)
    set_cell(t.cell(1, 0), "作品类别")
    set_cell(t.cell(1, 3), "人工智能应用")
    set_cell(t.cell(1, 10), "作品小类")
    set_cell(t.cell(1, 12), "实践赛")
    set_cell(t.cell(2, 0), f"作品简介（100字以内）\n{SUMMARY_100}")
    set_cell(t.cell(3, 0), f"创新说明（100字以内）\n{INNOVATION_100}")

    author_headers = ["项目", "成员1（待补）", "成员2（待补）", "成员3（待补）", "成员4（待补）", "成员5（待补）"]
    for i, name in enumerate(author_headers):
        set_cell(t.cell(6, min(i + 1, 16)), name if i else "项目")
    tasks = ["组织协调", "作品策划", "作品开发", "界面设计", "文档撰写", "测试分析"]
    for r, task in enumerate(tasks, start=7):
        set_cell(t.cell(r, 0), task)
        set_cell(t.cell(r, 2), PLACEHOLDER)
        set_cell(t.cell(r, 6), PLACEHOLDER)
        set_cell(t.cell(r, 9), PLACEHOLDER)
        set_cell(t.cell(r, 11), PLACEHOLDER)
        set_cell(t.cell(r, 14), PLACEHOLDER)

    set_cell(t.cell(14, 0), "指导教师情况")
    set_cell(t.cell(14, 5), "【待补：指导教师姓名、指导内容、资源支持情况】")

    set_cell(t.cell(15, 0), "运行平台")
    set_cell(t.cell(15, 4), "Windows、Linux（推荐）；具备 Python、CodeQL 与 Docker 环境的开发机或服务器均可运行")
    set_cell(t.cell(16, 0), "展示平台")
    set_cell(t.cell(16, 4), "Windows、Linux、macOS 浏览器端展示；支持以 Web 控制台方式进行现场演示")
    set_cell(t.cell(17, 0), "开发环境说明")
    set_cell(t.cell(17, 4), "系统后端基于 Python 构建多Agent安全分析流水线，前端采用 Vue 实现可视化控制台，依赖 CodeQL、Docker 等组件，适合在具备安全分析与容器执行能力的环境中部署。")
    set_cell(t.cell(18, 0), "参考作品（前3项）")
    set_cell(t.cell(18, 4), "1. GitHub CodeQL（代表性静态分析框架）\n2. Semgrep（轻量规则扫描工具）\n3. OWASP Benchmark（国际通用评测基准）")
    set_cell(t.cell(19, 0), "提交材料")
    set_cell(t.cell(19, 4), "作品报告、作品信息概要、源代码、演示视频、PPT（如有）、部署说明/用户手册（建议补充）")
    set_cell(t.cell(20, 0), "附加文件说明")
    set_cell(t.cell(20, 4), "建议同步提交安装部署说明、用户手册、运行依赖清单、第三方开源组件说明及素材来源说明，以增强评审复现性和材料完整度。")

    for idx, (name, status, copyright_) in enumerate(FILES, start=22):
        set_cell(t.cell(idx, 0), str(idx - 21))
        set_cell(t.cell(idx, 1), name)
        set_cell(t.cell(idx, 8), status)
        set_cell(t.cell(idx, 13), copyright_)

    set_cell(t.cell(29, 0), "本页签字与日期需参赛成员线下确认后补填。\n日期：" + TODAY + "（技术内容已填，签字待补）")
    doc.save(dst)


def fill_report_doc():
    src = BASE / "05-3 作品报告（人工智能实践赛）模板.docx"
    dst = BASE / "05-3 作品报告（人工智能实践赛）填写版-终稿强化.docx"
    copyfile(src, dst)
    doc = Document(dst)

    # cover
    doc.paragraphs[4].text = f"作品名称：{TITLE}"
    doc.paragraphs[7].text = f"填写日期：{TODAY}"

    replacements = {
        11: REPORT_OVERVIEW,
        24: REPORT_BACKGROUND,
        27: "本作品面向传统漏洞检测领域长期存在的‘高误报、难扩展、弱验证’问题，目标是在保证检测能力的基础上，通过多 Agent 协同与神经符号融合显著提升结果质量、规则生成效率与证据可信度，形成可复查、可验证、可落地的安全分析闭环。",
        30: "现有传统规则型工具强调严谨性但灵活性不足，纯大模型方案强调语义性却缺乏稳定约束，因此亟需一种兼具人工智能智能性、静态分析严谨性与动态验证可信性的综合技术路线。",
        34: REPORT_DESIGN,
        38: REPORT_TECH,
        42: REPORT_IMPL,
        46: REPORT_TEST,
        50: REPORT_SUMMARY,
        51: "作品特色与创新\n" + REPORT_FEATURES,
        53: "应用推广\n" + REPORT_PROMOTION,
        56: "作品展望\n" + REPORT_FUTURE,
        62: "[1] OWASP Foundation. OWASP Benchmark Project.\n[2] GitHub. CodeQL Documentation.\n[3] Semgrep Documentation.\n[4] 项目现有测试结果与系统实现材料。",
    }
    for idx, text in replacements.items():
        doc.paragraphs[idx].text = text

    # clear instructional/example paragraphs
    for idx in [12, 13, 14, 15, 17, 18, 19, 21]:
        if idx < len(doc.paragraphs):
            doc.paragraphs[idx].text = ""

    # fill comparison table
    table = doc.tables[0]
    headers = ["维度", "传统 SAST 工具", "纯 LLM 漏洞方案", "Argus"]
    for c, h in enumerate(headers):
        set_cell(table.cell(0, c), h)
    rows = [
        ["核心能力", "规则匹配与静态分析", "语义理解与文本推理", "多 Agent 协同 + 神经符号融合"],
        ["准确性", "容易出现较多误报", "上下文理解强但不稳定", "静态分析严谨且经语义审查优化"],
        ["结果可信性", "通常缺少动态验证", "容易出现幻觉或不可复现结论", "支持沙箱验证与证据链输出"],
        ["规则扩展", "依赖人工编写规则", "生成灵活但可用性不稳", "模板约束 + 编译自修复"],
        ["落地性", "工程成熟但智能不足", "创新性强但生产可用性有限", "兼顾创新性、工程性与展示性"],
    ]
    for r, row in enumerate(rows, start=1):
        for c, val in enumerate(row):
            set_cell(table.cell(r, c), val)

    doc.save(dst)


def fill_signup_doc():
    src = BASE / "附件2_2026年西北地区赛作品报名表.docx"
    dst = BASE / "附件2_2026年西北地区赛作品报名表_填写版.docx"
    copyfile(src, dst)
    doc = Document(dst)
    t = doc.tables[0]

    set_cell(t.cell(0, 1), TITLE)
    set_cell(t.cell(1, 4), "人工智能应用")
    set_cell(t.cell(1, 9), "【待补：参赛小类/组别】")
    set_cell(t.cell(2, 1), "【待补：学校名称】")
    set_cell(t.cell(3, 1), "【待补：学校联系人】")
    set_cell(t.cell(3, 8), "【待补：联系电话】")

    for row in [5, 6, 7]:
        set_cell(t.cell(row, 2), f"【待补：成员{row-4}姓名】")
        set_cell(t.cell(row, 5), "【待补】")
        set_cell(t.cell(row, 7), "【待补：院系】")
        set_cell(t.cell(row, 8), "【待补：专业】")
        set_cell(t.cell(row, 10), "【待补：电话】")
        set_cell(t.cell(row, 12), "【待补：签字】")

    set_cell(t.cell(11, 2), "【待补：指导教师姓名】")
    set_cell(t.cell(11, 5), "【待补】")
    set_cell(t.cell(11, 7), "【待补：电话】")
    set_cell(t.cell(11, 12), "【待补：签字】")

    set_cell(t.cell(13, 0), "作品内容简介（100字以内）\n" + SUMMARY_100)
    set_cell(t.cell(14, 0), "创意思路（约500字以内）\nArgus 围绕当前漏洞检测中‘静态规则工具误报率高、人工审查成本大、纯大模型方案结果不稳定’的问题，提出了一种以神经符号融合为核心的多 Agent 协同自动化漏洞检测方案。系统并未将大模型直接作为最终裁决者，而是将其能力限定在规划、规则生成、语义审查和验证编排等高层环节；底层精确检测由 CodeQL 负责执行，最终再通过 Docker 沙箱完成动态验证，形成可追溯、可复查的证据链。\n\n在系统结构上，作品设计了六类 Agent，分别负责代码库侦察与扫描规划、项目分诊、规则合成、告警语义研判、PoC 生成和沙箱执行。通过这种职责拆分，系统既保留了人工智能的灵活性，又继承了符号分析的严谨性。与此同时，作品构建了带信任分级与完整性保护机制的规则记忆库，使系统能够利用历史高质量规则与漏洞经验，又能降低知识投毒风险。\n\n从应用价值看，Argus 可用于企业级 DevSecOps、安全教学、攻防演练与开源项目审计等场景。作品既体现了人工智能技术在安全分析中的实际落地，也强调结果的可解释性、可验证性与工程可用性，符合人工智能实践赛对‘创新性 + 实用性 + 完整性’的综合要求。")
    set_cell(t.cell(15, 0), "单位意见\n【待补：学院/学校审核意见并加盖公章】")
    doc.save(dst)


def fill_originality_doc():
    src = BASE / "附件3_2026年西北地区赛作品原创性声明.docx"
    dst = BASE / "附件3_2026年西北地区赛作品原创性声明_填写版.docx"
    copyfile(src, dst)
    doc = Document(dst)
    doc.paragraphs[2].text = "郑重声明："
    doc.paragraphs[3].text = f"我们提交参赛的作品《{TITLE}》，是在指导教师指导下独立完成的研究与实践成果。除在文中已经明确标注引用来源的内容外，本作品不包含任何他人已公开发表或完成的成果，不存在侵犯他人知识产权的情形。"
    doc.paragraphs[4].text = "我们是本作品的主要创作者和贡献者，已充分知悉并认可赛事对原创性、真实性和合规性的相关要求，愿意承担与作品内容、材料提交和知识产权有关的全部责任。"
    doc.paragraphs[7].text = "参赛成员签名：\n【待补：全体成员签字】"
    doc.paragraphs[8].text = "指导教师签字：\n【待补：指导教师签字】"
    doc.paragraphs[10].text = f"日期：{TODAY}（如需按实际签署日期修改，请手动更新）"
    doc.save(dst)


def main():
    fill_summary_doc()
    fill_report_doc()
    fill_signup_doc()
    fill_originality_doc()
    print('done')


if __name__ == '__main__':
    main()
